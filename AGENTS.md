# Repository Instructions

## Release Discipline

- Every MR/PR created for this repository must include a package version bump.
- For Agent Bridge changes, bump `agent-bridge/package.json` so the publish workflow can publish a new npm version after the MR lands.
