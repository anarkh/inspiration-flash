# Token Relay Protocol

Shared TypeScript wire contracts and validation helpers for Token Relay.

This package is the runtime protocol dependency of
`@anarkhli/provider-sdk`. Most Provider operators should install the SDK
instead of depending on this package directly.

```bash
npm install @anarkhli/protocol
```

Requires Node.js 22.13 or newer.
